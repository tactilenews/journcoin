// import ArticlePreview from './ArticlePreview.vue'

describe('ArticlePreview', () => {
  describe('shallowMount', () => {
    describe('button', () => {
      it.todo('emits `buy(slug)`')
      describe('given article bought by user', () => {
        it.todo('emits `read(slug)`')
      })
      describe('given article written by current user', () => {
        it.todo('emits `read(slug)`')
      })
    })
  })
})
